# 💻 하드웨어와 메모리의 관계 및 운영체제 관리 메커니즘

## 1. 환경별 데이터 흐름 및 관리 전략

### 🎮 유저(클라이언트) 입장
* **초기 상태**: 게임 설치 후 모든 데이터는 보조기억장치(**SSD/HDD**)에 존재.
* **실행 (Loading)**: 가상 메모리 주소 체계를 통해 SSD 데이터를 **RAM으로 복사**. OS의 메모리 관리자가 이 과정을 조율함.
* **메모리 부족 (Paging)**:
    * **Page Out**: RAM 부족 시, OS가 현재 사용하지 않는 RAM 데이터 일부를 SSD/HDD로 옮김.
    * **Page In**: 해당 데이터가 다시 필요해지면 RAM으로 가져옴. 이때 SSD/HDD는 RAM보다 훨씬 느리므로 시스템 **'버벅임(Stuttering)'** 발생.

### 🖥️ 서버 입장 (실시간성 & 데이터 무결성 핵심)
* **서버 시작 (Init)**: DB에 있는 아이템 정보, 몬스터 스폰 등 설정값 전부를 **RAM에 상주(In-Memory)** 시킴.
* **최적화 목표**: 로딩 속도보다 응답 속도가 중요하므로, Page Out/In이 발생하여 디스크를 헤매는 시간을 최소화해야 함.
* **데이터 저장 (Persistence)**:
    * 유저 상태 변화 발생 시 서버 프로세스의 **가상 메모리(RAM) 정보 갱신**.
    * 특정 주기마다 **DB 동기화 및 SSD 로그(Log)** 기록을 통해 영속성 확보.
      * 데이터 중요도에 따라 적합한 백업이 필요. (즉시기록, 주기적 저장 및 로그, 트랜잭션과 시퀀스번호, 롤백)
* **메모리 관리**: 서버는 안정성이 최우선. Swap 발생 시 Ping이 커져 유저 이탈 발생. 메모리 모니터링을 통해 **Scale Up/Out** 고려 필수.

---

## 2. OS 커널의 동작 원리 (Deep Dive)

모든 작업은 **OS 커널**이 가상 메모리 관리 시스템 내의 **페이지 테이블(Page Table)** 을 참조하여 진행함.

### ⚡ 데이터 접근 시나리오
1.  **RAM에 데이터 있음**: CPU에 즉시 전달.
2.  **RAM에 데이터 없음 (Page Fault)**:
    * 1번 스레드가 데이터를 요청했으나 RAM에 없음 확인.
    * **CPU(OS)** 가 SSD 컨트롤러에 "RAM 특정 위치로 데이터를 옮겨달라"고 명령.
    * **1번 스레드 상태**: **대기(Waiting/Blocked)** 상태로 전환 (동기 블로킹).
    * **데이터 복사**: CPU 대신 **DMA(Direct Memory Access)**가 SSD와 RAM 사이에서 처리를 전담.
    * **완료 알림**: I/O가 완료되면 DMA가 CPU에 **인터럽트(Interrupt)** 신호를 보냄.
    * **재개**: OS가 대기 상태였던 1번 스레드를 **Ready**로 전환하여 연산 재개.

> **참고**: 비동기 논블로킹은 프로그래밍 레벨에서 대기 시간 동안 다른 작업을 하도록 설계하는 것이나, 실제 OS 최하단 레벨의 물리적 동작은 위와 같은 동기적 흐름을 기반으로 함.

---

## 3. 메모리 계층 구조 요약 (Hierarchy)

1.  **실행**: SSD/DB 데이터를 **RAM**에 로드.
2.  **연산**: CPU가 필요한 데이터를 RAM에서 덩어리(Cache Line)로 **캐시**로 가져옴.
3.  **Cache Miss**: 캐시에 없으면 RAM을 뒤짐 (느림).
4.  **Page Fault**: RAM에 없으면 SSD를 뒤짐 (**Page In**). 매우 느림.
5.  **Page Swap**: RAM 용량 초과 시 안 쓰는 영역을 SSD/HDD로 보냄. 매우 느림.

---

## 4. 아키텍처 비트(Bit) 별 특징

| 구분 | 32bit 환경 | 64bit 환경 |
| :--- | :--- | :--- |
| **용량 및 주소** | 최대 4GB 한계 (유저 영역 2GB). 매우 협소함. | 최대 128TB 이상 가용. 매우 넉넉함. |
| **보안성** | 주소 공간이 좁아 데이터 위치 예측이 상대적으로 쉬움. | 주소 공간이 광활하여 특정 데이터 위치를 찾기 힘듦 (ASLR 효율 극대화). |
| **비유** | 도둑이 들어왔을 때 먹잇감이 바로 옆에 밀집한 상태. | 도둑이 들어왔으나 사막에서 바늘을 찾아야 하는 상태. |

---

## 🖥️ 가상 메모리 및 시스템 아키텍처 핵심 정리

### 1. 가상 메모리(Virtual Memory)란?
 - 정의: 물리 메모리(RAM)의 한계를 극복하기 위해, OS가 프로세스에게 대용량의 독립된 메모리를 가지고 있는 것처럼 속이는 메모리 관리 기법.

 - 핵심 원리: 프로세스는 가상 주소를 사용, 하드웨어(MMU)와 OS가 이를 실제 물리 주소로 번역. 이를 통해 각 프로세스는 서로의 메모리를 침범할 수 없는 독립적인 공간을 가짐.

### 2. 운영체제별 가상 메모리 할당 크기
 - 32bit 운영체제 (포인터 크기 4byte): 2^32의 주소 체계를 가지므로 프로세스당 최대 4GB의 가상 메모리를 할당받습니다.

 - 64bit 운영체제 (포인터 크기 8byte): 2^64의 주소 체계를 가지므로 이론상 약 16EB(엑사바이트)까지 가능하지만, 실제로는 구현상의 한계로 수 TB(테라바이트) 단위의 광활한 공간을 사용합니다.

### 3. 할당받는 메모리의 공간 분할 (Address Space Split)
 - 가상 메모리는 프로그램만 쓰는 게 아니라 OS와 나누어 사용.

 - 유저 영역(User Space): 프로그램의 코드, 데이터, 힙, 스택이 쌓이는 곳. (32bit 기준 보통 2GB)

 - 커널 영역(Kernel Space): OS 핵심 코드가 상주하는 곳. 유저 프로그램이 시스템 콜을 보낼 때 주소 공간 전환 없이 빠르게 처리하기 위해 같은 가상 주소 공간에 미리 매핑된 상태.

### 4. RAM 선택 기준과 현대적 구성 (8G+8G, 16G+16G)
 - 선택 기준: 32bit OS는 4GB 이상의 RAM을 인식하지 못하므로 4GB가 한계. 반면 64bit OS는 물리적 RAM을 거의 제한 없이 인식하므로 다다익램이 가능하다.

 - 현대적 구성의 이유:

 - 멀티태스킹: 64bit 환경에서는 수많은 프로세스가 각각 광활한 가상 메모리를 가진다. 물리 RAM이 많을수록 실제 RAM에 올라와 있는 프로세스가 많아져 시스템이 쾌적해짐.

 - 듀얼 채널 효과: 8G+8G처럼 짝수로 꽂는 이유는 대역폭을 넓혀 데이터를 한 번에 더 많이 주고받기 위함입니다.

 - 가상 메모리와의 관계: 물리 RAM이 부족하면 OS는 부족한 부분을 디스크(Swap)에 기록하게 된다. RAM을 넉넉히(16G+16G 등) 꽂는 것은 상대적으로 느린 Swap의 I/O를 최소화하려는 전략.

### 5. 페이징과 컨텍스트 스위칭 (조건 및 비용)
 - 페이징(Paging): 가상 메모리를 일정한 크기(Page)로 나누어 관리하는 기법입니다. 4KB or 8KB

 - 컨텍스트 스위칭(Context Switching) 발생 조건:

 - 멀티태스킹: CPU가 한 프로세스에서 다른 프로세스로 제어권을 넘길 때.

 - 모드 전환: 유저 모드에서 시스템 콜을 통해 커널 모드로 진입할 때.

 - 페이지 폴트(Page Fault): 요청한 가상 메모리 페이지가 실제 RAM에 없어 디스크에서 가져와야 할 때.

 - 비용(Cost): CPU 레지스터 상태 저장 및 복구 비용.

 - 가장 큰 비용 : **캐시 메모리(TLB, CPU Cache)의 초기화(Flush)**. 새로운 프로세스의 메모리 지도가 들어오면 기존의 캐시 데이터가 무효화됨. 시스템 성능이 순간적으로 저하된다.

## 🛡️ 가상 메모리 레이아웃과 오버플로우(Overflow)

### 1. 가상 메모리 영역의 할당과 방향
 - 프로그램이 실행되면 OS는 가상 메모리를 크게 4가지 영역으로 분리. 메모리 공간을 효율적으로 사용하기 위해 서로 마주 보며 자란다.

| 영역 | 주소 방향 | 특징 |
| :--- | :--- | :--- |
| **코드 영역** | 낮은 주소 | 기계어, 명령어가 저장됨 (Read-Only) |
| **데이터 영역** | 낮은 주소 | 전역 변수, 정적 변수(static) 저장 |
| **힙 영역** | 낮은 -> 높은 | 런타임에 동적 할당. 위로 자람 |
| **스택 영역** | 높은 -> 낮은 | 지역 변수, 매개 변수, 리턴 주소. 아래로 자람 |

### 힙과 스택이 반대로 자라는 이유
 - 가상 메모리라는 유한한 공간 안에서 힙과 스택 중 어느 쪽이 많이 쓰일지 미리 알 수 없기 때문. 양 끝단에서 시작해 서로를 향해 자라게 함으로써, 중간의 빈 공간을 공유하며 메모리를 최대한 유연하게 사용하기 위함.

### 2. 오버플로우(Overflow)란 무엇인가?
 - 정해진 메모리 영역의 경계를 넘어서 데이터가 써지는 현상을 말합니다. 인접한 메모리를 오염시키기 때문에 매우 위험합니다.

### 🚩 오버플로우의 종류
- Stack Overflow:
  - 원인: 재귀 호출이 너무 깊거나, 스택에 너무 큰 지역 변수(예: int arr[1000...000])를 선언할 때 발생.
  - 결과: 스택 포인터가 스택 할당량을 넘어서 힙 영역이나 유효하지 않은 메모리를 침범.

- Heap Overflow:
  - 원인: 동적으로 할당된 버퍼보다 큰 데이터를 쓸 때 발생.
  - 결과: 인접한 힙 청크(Chunk)의 메타데이터를 파괴하여 프로그램 크래시나 권한 상승 공격에 이용됨.

- Buffer Overflow:
  - 원인: 데이터가 저장되는 '버퍼'의 크기보다 많은 양의 데이터가 입력될 때 발생. (스택, 힙, 데이터 영역 어디서든 발생 가능)

- Integer Overflow:
  - 원인: 정수형 타입이 표현할 수 있는 최대치($2^{31}-1$ 등)를 넘어서 값이 갑자기 최소값으로 바뀌는 현상.
  - 메모리 할당 크기 계산 시 버그를 유발함.
  
### 3. 버퍼 오버플로우의 심층 분석

### 🧐 언제 생기는가?

- 입력값의 길이를 검증하지 않는 함수(gets, strcpy, scanf 등)를 사용할 때 주로 발생.

- 사용자가 의도적으로 버퍼 크기보다 긴 문자열을 입력하면 인접한 메모리 주소를 덮어쓰게 된다.

### 🚫 컴파일 및 크래시 발생 여부
- 컴파일 시: 대부분 발생하지 않는다. 버퍼 오버플로우는 실행 중에 데이터가 들어올 때 발생하는 런타임 에러이기 때문. (최신 컴파일러가 위험한 함수 사용 시 경고를 주긴 하지만 빌드를 막지는 않음)

- 크래시(Crash) 발생 조건:
  - 덮어쓴 위치가 접근 권한이 없는 메모리이거나, 함수의 **리턴 주소(Return Address)**를 이상한 곳으로 바꿔 CPU가 잘못된 명령어를 읽으려 할 때 발생 (Segmentation Fault).

- 크래시가 발생하지 않는 조건 (최악):
  - 덮어쓴 위치가 여전히 해당 프로세스의 유효한 메모리 범위 내라면 프로그램은 죽지 않고 계속 실행되지만, 언제 터질지 모르고 어떤 문제를 발생시킬지 모름. 예상 가능한 버그가 훨씬 낫다.

### ⚠️ 크래시가 나지 않을 때의 위협
- 데이터 오염: 옆에 있던 다른 변수의 값이 갑자기 바뀌게 된다. (로그인 여부 변수가 false에서 true로 바뀌는 등)

- 제어 흐름 탈취: 함수의 리턴 주소를 공격자가 심어놓은 악성 코드의 주소로 변형 가능. 프로그램이 죽지 않고 자연스럽게 공격자의 코드를 실행하게 됨.

## 🛡️ 메모리 오버플로우 방어 체계 및 보안 설정 (Defense in Depth)
- 메모리 보안은 어느 한 단계에서만 이뤄지는 것이 아니라, 코드-컴파일-운영체제가 계층적으로 협력하여 방어한다.

### 1단계. 소프트웨어 레벨: 예방 (Secure Coding)
개발자가 코드를 작성할 때 가장 먼저 고려해야 할 방어선.

- 안전한 함수 사용: 길이를 체크하지 않는 strcpy, strcat, gets 대신, 최대 길이를 명시하는 strncpy, strncat, fgets 등을 사용해야 한다.

- 컨테이너 활용 (C++): 로우레벨 배열(char[]) 대신 크기 관리가 자동인 std::vector, **std::string**을 사용하며, 필요하다면 접근 시 .at()을 통해 범위 검사를 수행.

- 빌드 옵션 (SDL 검사): * Visual Studio: /sdl (Security Development Lifecycle) 옵션을 켜면 취약한 함수 사용 시 경고가 아닌 에러를 발생시켜 빌드를 차단한다.

### 2단계. 컴파일러 레벨: 감지 (Detection)
코드가 빌드되어 바이너리가 될 때, 오버플로우를 감지하는 장치를 삽입.

- Stack Canary (Stack Guard): 함수의 복귀 주소(Return Address) 바로 앞에 '카나리'라는 특수한 값을 심고, 함수 종료 시 이 값이 변했다면 즉시 프로그램을 종료한다.
  - Visual Studio: /GS (기본 활성)
  - GCC/Clang: -fstack-protector

- Control Flow Guard (CFG): 함수 포인터 등을 이용한 간접 호출 시, 대상 주소가 정상적인 범위인지 확인하여 제어 흐름 탈취를 막는다.
  - Visual Studio: /guard:cf
 
### 3단계. OS 및 하드웨어 레벨: 차단 (Protection)
공격자가 메모리를 덮어쓰는 데 성공하더라도, 실행에 실패하게 만드는 최종 방어선.

- ASLR (Address Space Layout Randomization): 실행 시마다 스택, 힙, 라이브러리 주소를 무작위로 배치하여 공격자가 주소를 예측하지 못하게 한다.
  - Visual Studio: /DYNAMICBASE
  - GCC/Clang: -fPIE -pie

- DEP / NX Bit (Data Execution Prevention): 스택이나 힙 같은 데이터 영역에 **'실행 권한'**을 제거합니다. 공격 코드가 주입되어도 실행되지 않도록.
  - Visual Studio: /NXCOMPAT
  - GCC/Clang: -z noexecstack

## 💡 요약: 플랫폼 개발자의 방어 전략

| 단계 | 방어 기법 | 주요 빌드 옵션 (VS / GCC) | 비고 |
| :--- | :--- | :--- | :--- |
| **예방** | 안전한 함수, 길이 검증 | /sdl | 코드 레벨의 근본적 해결 |
| **감지** | Stack Canary | /GS , -fstack-protector | 메모리 오염 시 즉시 종료 |
| **차단** | ASLR (주소 무작위화) | /DYNAMICBASE , -pie | 공격 대상의 위치를 숨김 |
| **차단** | DEP/NX (실행 방지) | /NXCOMPAT , -z noexecstack | 데이터 영역 코드 실행 차단 |


## 🔍 예시: 배열 및 STL 범위를 벗어난 접근

### 1. 스택 배열: int arr[100] 선언 후 arr[200] 접근
- 오버플로우 종류: 스택 버퍼 오버플로우 (Stack Buffer Overflow)

- 스택 내 특정 버퍼(배열)의 경계를 넘어서 인접한 메모리 영역을 침범함.

- 컴파일 여부: 성공

- C/C++ 표준은 성능을 위해 런타임 배열 인덱스 검사를 수행하지 않음.

- 크래시 발생 여부: 미확정 (발생하지 않을 가능성 높음)

- 발생 안 함: arr[200] 위치가 프로세스에게 할당된 스택 페이지(Page) 범위 내라면 OS는 이를 정상적인 접근으로 간주함. (데이터 오염 발생)

- 발생 함: 접근한 주소가 접근 권한이 없는 다른 페이지이거나, 리턴 주소(Return Address) 오염으로 인해 잘못된 명령어로 점프했을 때 Access Violation 크래시 발생.

- 감지에 의한 발생: 빌드 옵션 /GS가 켜져 있다면 함수 종료 시 Stack Canary 변조를 감지하여 "Stack Buffer Overrun" 메시지와 함께 안전하게 종료됨.

### 2. STL 컨테이너: std::vector<int> v(100) 후 v[200] 접근
- 오버플로우 종류: 힙 버퍼 오버플로우 (Heap Buffer Overflow)

- std::vector는 내부 데이터를 힙(Heap) 영역에 할당하기 때문임.

- 컴파일 여부: 성공

- operator[]는 속도를 위해 경계 검사를 수행하지 않음.

- 크래시 발생 여부: 미확정 (가장 위험한 케이스)

- 발생 안 함: 힙의 인접한 빈 공간이나 다른 동적 할당 객체의 데이터를 조용히 덮어씀. (추적하기 매우 힘들다)

- 발생 함: 힙의 메타데이터를 파괴하여 나중에 delete/free를 호출할 때 터지거나, 유효하지 않은 힙 페이지를 건드릴 때 발생.

- 참고: v.at(200)을 사용하면 즉시 std::out_of_range 예외가 발생하여 크래시(예외 종료)를 통해 문제를 즉각 알 수 있음.

### 3. STL의 최대 크기 예측 및 제한
- T(데이터 타입)마다 다른 이유: 메모리는 '바이트' 단위로 관리되므로, 객체 하나의 크기(sizeof(T))가 클수록 담을 수 있는 개수는 줄어듦.

- 최대 크기 확인 방법:

- v.max_size(): 시스템 아키텍처와 라이브러리 구현상 할당 가능한 최대 요소 개수를 반환함.

- 예측 및 제한 요소:

- 가상 메모리 주소 공간: 32비트 환경에서는 커널 영역을 제외한 최대 2GB가 한계임.

- 물리 RAM 및 스왑 파일: 실제 물리 램보다 큰 데이터를 할당하려고 하면 Page Fault가 빈번해져 속도가 급격히 느려지다가, 결국 할당 실패(std::bad_alloc) 발생.

- 연속된 메모리 공간: std::vector는 모든 데이터를 연속된 공간에 배치해야 하므로, 총 여유 메모리가 많아도 '큰 조각 하나'가 없으면 할당에 실패할 수 있음 (메모리 단편화 문제).

## 정리
왜 크래시가 안 나면 더 위험한가?
- 프로그램이 즉시 죽지 않고 잘못된 데이터로 계속 실행된다. 나중에 원인을 알 수 없는 오동작을 유발, 공격자가 리턴 주소를 변조하여 시스템 권한을 탈취하는 통로가 된다.

## 연습 문제
unordered_map 의 크기는?

- unordered_map<int64, std::array<int, 10000>> hashMap을 무한루프를 돌며 확인
- hashMap.max_size() = 107331 -> 32bit 가상 주소 공간 약 4GB 크기.
- 32bit 환경 기준 프로세스 메모리 2.1G에서 bad_alloc (hashMap key count : 52819 대략 2GB) -> 32bit 가상 주소 공간 실제 유저가 사용할 수 있는 크기.
- 물리 RAM이 32GB여도 상관 없다. 32bit 프로세스는 주어진 가상 주소 공간이 4GB 이고 유저영역이 약 2GB뿐이라, 40KB 짜리 공간을 더 할당할 수 없어서 죽는다.

map 의 크기는?
- unordered_map과 큰 차이 없다.
- max_size() = 107309 , bad_alloc 발생 지점 = 52825.