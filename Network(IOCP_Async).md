# 🏆 IOCP 비동기 논블로킹 시스템 최종 정리

## 🚀 IOCP와 하드웨어 협업 메커니즘

### 1. 메모리 관점의 혁신: "Zero-Copy와 가상 메모리 매핑"
기존의 `send`/`recv`가 **'데이터 복사'** 에 집중했다면, IOCP는 **'자원 공유'** 와 **'직접 접근'** 에 집중한다.

* **가상 메모리 고정 (Memory Pinning)**: `WSASend`/`WSARecv` 호출 시, OS 커널은 유저가 전달한 `WSABuf` 주소의 가상 메모리 페이지를 물리 RAM에 고정한다. 이로 인해 I/O 도중 데이터가 SSD로 스왑(Page Out)되는 현상을 방지.
* **Zero-Copy (SO_SNDBUF = 0)**:
    * **일반 소켓**: [유저 RAM] → (CPU가 복사) → [커널 TCP 버퍼 RAM] → [NIC]
    * **IOCP**: [유저 RAM] → (DMA가 직접 읽기) → [NIC]
    * **결과**: CPU가 단순 복사 노동에서 해방되어 순수 비즈니스 로직 연산에만 전념할 수 있습니다.

### 2. 하드웨어의 협업: "전담 비서 DMA와 인터럽트"
* **주문서 전달**: 애플리케이션이 `WSASend`를 호출하는 것은 OS에게 "이 주소(`WSABuf`)에 데이터가 있으니 전송해줘"라고 주문서를 넣는 예약 행위.
* **비동기 실행**: 주문 후 CPU는 즉시 다음 코드를 실행하며, 실제 전송은 하드웨어에 맡깁니다.
* **DMA의 활약**: CPU가 다른 작업을 처리하는 동안, **DMA(Direct Memory Access)** 는 RAM에 고정된 `WSABuf`에서 데이터를 읽어 **네트워크 카드(NIC)** 로 직접 전송합니다.
* **인터럽트와 완료**: 전송 완료 시 NIC가 CPU에 인터럽트를 걸고, OS는 이를 확인하여 **IOCP 큐(Completion Queue)** 에 "작업 완료" 패킷을 삽입합니다.

### 3. 네트워크 흐름 제어 최적화 (Read Speed 극대화)
* **Pre-posting (미리 던져놓기)**: `WSARecv`를 미리 여러 개 등록해 두는 것은, 데이터 도착 시 중간 거점(커널 버퍼) 없이 유저 RAM 주소로 **직접 배달**하라는 통로를 미리 열어두는 것.
* **Window Size 유지**: 커널 버퍼에서 유저 버퍼로의 복사 단계가 생략되거나 비약적으로 빠르므로, TCP의 **수신 윈도우(Window Size)** 가 항상 넉넉하게 유지된다. 이는 상대방(송신자)이 멈추지 않고 데이터를 계속 보낼 수 있게 만든다.

---

## 🚀 시스템

### 1. 하드웨어 및 가상 메모리 활용
- **WSABuf 매핑**: 유저 가상 주소를 커널과 공유하여 물리 RAM에 고정(Pinning), I/O 중 페이지 폴트(Page Fault)를 근본적으로 차단함.
- **DMA 전송**: CPU 개입 없이 DMA가 RAM과 NIC 사이에서 데이터를 직접 운반하여 CPU 오버헤드를 제로화함.

### 2. TCP 스택과 흐름 제어
- **Zero-Copy**: `SO_SNDBUF=0` 설정을 통해 불필요한 커널 복사를 제거하고 메모리 대역폭을 보존함.
- **처리량 최적화**: 데이터 도착 즉시 유저 메모리에 직접 기록함으로써 'Read Speed'를 하드웨어 한계치까지 끌어올림.

### 3. 혼잡 제어 및 안정성
- **응답성 향상**: CPU 부하가 낮아져 네트워크 혼잡(Congestion) 상황에 더 민감하게 대응하며, 대량의 패킷을 지연 없이 처리함.

### 4. 64bit 환경의 이점
- **광활한 주소 공간**: 수만 개의 커넥션과 각각의 `WSABuf`가 겹치지 않게 가상 주소를 할당할 수 있음.
- **RAM 상주**: 모든 활성 소켓 버퍼를 실제 RAM에 상주시켜 디스크 스왑 없이 고속 통신을 보장함.

---

## 요약

|단계|	주체	|역할|
| :--- | :--- | :--- |
|요청|	CPU (애플리케이션)|	"이 데이터를 보내라" 명령 (WSASend)|
|준비|	OS (커널)|	메모리 고정(Pinning) 및 DMA 주문서 작성|
|전송|	DMA & NIC (하드웨어)|	실제 RAM 데이터를 읽어 네트워크로 송출|
|보고|	NIC → CPU (인터럽트)|	"전송 완료!"|

### 💡 결론
IOCP는 **[가상 메모리 고정] + [DMA 직접 접근] + [TCP 흐름 제어 최적화]**를 하나로 통합한 정교한 메커니즘입니다. 개발자는 복잡한 데이터 전송 과정을 OS와 하드웨어에 위임하고, 오직 `GetQueuedCompletionStatus()`를 통해 전달되는 '결과'에만 집중하면 됩니다.

# 📝 [연습문제] 패킷 크기 및 옵션에 따른 IOCP 비동기 I/O 흐름 분석 (종합)

## 1. 상황 설정
* **환경**: 64bit Windows Server (IOCP 모델)
* **송신 데이터**: 1 byte / 1,000 byte / 100,000 byte
* **비교 설정**: 
    1. **Zero-Copy 활성** (`SO_SNDBUF = 0`)
    2. **Zero-Copy 비활성** (기본값, 커널 버퍼 사용)

---

## 2. Zero-Copy 활성화 vs 비활성화 상세 비교

### [상태 1] Zero-Copy 활성화 (`SO_SNDBUF = 0`)
* **등록 시점**: 커널은 유저 메모리 주소만 접수하고 페이지를 고정(**Pinning**)함.
* **전송 과정**: **CPU는 복사하지 않음.** DMA 하드웨어가 유저 RAM에서 데이터를 직접 읽어 NIC로 쏨.
* **완료 시점**: 하드웨어가 데이터를 **실제로 다 보냈을 때** 완료 통지 발생.
* **장점**: CPU 사용률 최저, 대용량 전송에 유리.
* **단점**: 전송 완료 전까지 유저 메모리를 절대 건드릴 수 없음 (메모리 점유 시간 김).

### [상태 2] Zero-Copy 비활성화 (커널 버퍼 사용)
* **등록 시점**: `WSASend` 호출 시, **CPU가 유저 메모리 데이터를 '커널 송신 버퍼'로 즉시 복사함.**
* **전송 과정**: 복사가 끝나면 유저 메모리의 역할은 끝남. 이후 커널이 알아서 DMA를 통해 NIC로 보냄.
* **완료 시점**: **커널 버퍼로의 복사가 완료되는 즉시** 완료 통지가 올 수 있음 (실제 전송 여부와 무관).
* **장점**: 유저 메모리를 빠르게 회수하여 재사용 가능. 소량 패킷 처리에 안정적.
* **단점**: 데이터가 클수록 CPU 복사 오버헤드와 커널 메모리 사용량 급증.

---

## 3. 패킷 크기 및 모드별 흐름 분석



### ① 1 byte (초소형 데이터)
* **활성 (Zero-Copy)**: 1바이트를 위해 페이지 고정 및 DMA 설정 오버헤드 발생. 비효율적일 수 있음.
* **비활성 (Default)**: CPU가 1바이트를 커널 버퍼로 순식간에 복사하고 즉시 완료 통지. 훨씬 빠르고 효율적임.

### ② 1,000 byte (중형 데이터)
* **활성 (Zero-Copy)**: DMA가 유저 RAM에서 직접 전송. 단일 패킷으로 나감.
* **비활성 (Default)**: 커널 버퍼로 1,000바이트 복사 후 완료 통지. 커널이 적절한 타이밍에 전송 수행.

### ③ 100,000 byte (대형 데이터 / 약 100KB)
* **활성 (Zero-Copy)**: **진가 발휘.** 100KB를 복사하는 CPU 연산이 전혀 없음. 하드웨어가 직접 수십 개의 패킷으로 쪼개서 송출하며, 전체 송출 완료 시 통지.
* **비활성 (Default)**: **CPU 비상.** CPU가 100KB를 커널 메모리로 복사하느라 자원을 소모함. 커널 송신 버퍼가 100KB보다 작다면, 버퍼에 자리가 날 때까지 `WSASend`가 대기하거나 나누어서 처리됨.

---

## 4. 비동기 함수의 반환값 및 완료 의미

### A. WSASend 호출 (등록/복사의 의미)
* **Zero-Copy 활성**: "주문 접수 완료, 이제 이 메모리는 하드웨어가 다 쓸 때까지 잠근다."
* **Zero-Copy 비활성**: "복사 완료, 이제 유저 메모리는 네가 다시 써도 된다. (실제 전송은 내가(OS, 커널) 알아서 할게.)"

### B. GetQueuedCompletionStatus (완료의 의미)
* **의미**: 
    - **활성**: "하드웨어가 실제 전송을 끝냈다." (물리적 전송 완료)
    - **비활성**: "커널 버퍼로 복사 작업이 끝났다." (논리적 전송 완료)

---

## 5. 수신측(Client)의 수신 흐름 및 순서 보장

### A. TCP의 순서 보장 (Ordering)
- **순서 유지**: 서버가 송신한 데이터(1B -> 1KB -> 100KB)는 중간에 패킷 순서가 뒤바뀌더라도 클라이언트 OS의 TCP Stack에서 **Sequence Number**를 기준으로 재조립되어 유저에게 전달됨.
- **Stream 방식**: TCP는 데이터의 경계(Boundary)를 구분하지 않음. 1B와 1KB가 합쳐져서 한 번에 읽힐 수도 있고, 100KB가 여러 조각으로 나뉘어 읽힐 수도 있음.

### B. IOCP 수신 (WSARecv)
- **Pre-posting**: 유저가 미리 `WSABuf`를 OS에 등록하여 수신 예약.
- **Direct Delivery**: 데이터가 도착하면 커널 버퍼를 거치지 않고(또는 최소화하여) 유저 RAM 주소로 **DMA가 직접 쓰기**를 수행.
- **GQCS 통지**: 데이터가 유저 버퍼에 다 채워진 후 소유권이 유저에게 반환됨.

### C. 수신측 병목 (Read Speed)
- 유저 애플리케이션이 `GQCS`로 완료 통지를 받았음에도 데이터를 빨리 처리하지 못하면, OS의 **수신 윈도우(Window Size)** 가 줄어듦.
- 결과적으로 서버의 송신 속도를 늦추게 됨 (**흐름 제어** 발생).

---

## 💡 송/수신 종합

1. **송신(Server)**: `WSASend`는 OS에게 데이터를 **"가져가라"** 고 하는 것. (Zero-Copy 시 하드웨어가 직접 가져감)
2. **수신(Client)**: `WSARecv`는 OS에게 데이터를 **"가져다 달라"** 고 예약하는 것. (DMA가 유저 RAM에 직접 꽂아줌)
3. **순서**: TCP는 물리적 패킷 순서와 상관없이 **논리적 데이터 순서(Sequence)** 를 완벽히 보장함.
4. **나(OS)의 역할**: 비활성 모드 송신 시에는 "대리인(Buffer)", 수신 시에는 "배달원(DMA 제어)" 역할을 수행함.

## 💡 Memory

1. **CPU 효율**: 대용량 패킷(100KB↑)은 **Zero-Copy**가 압도적으로 유리함.
2. **응답 속도**: 아주 작은 패킷은 **커널 버퍼(비활성)** 를 쓰는 것이 유저 메모리 회수 면에서 유리함.
3. **병목 지점**: 
    - Zero-Copy 시에는 **네트워크 대역폭**이 병목.
    - 비활성 시에는 **CPU 복사 속도 및 커널 메모리 자원**이 병목.