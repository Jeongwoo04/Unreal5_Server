## Network 이론 정리 (TCP UDP) 및 출처 영상

# 🌐 TCP 송수신 원리와 데이터 흐름의 본질

TCP/IP 통신 과정에서 데이터가 메모리와 네트워크 사이를 어떻게 이동하며, 성능 병목이 어디서 발생하는지 알아보자.

---

## 1. 송신 측(Sender)의 데이터 처리 과정

1. **User Buffer (애플리케이션 영역)**:
   - 프로그램이 파일(예: 1.4MB)을 읽어 메모리에 올린 상태.
2. **Kernel Buffer (TCP 송신 버퍼)**:
   - `send()` 함수 호출 시, 데이터가 유저 메모리에서 커널 내의 **TCP 송신 버퍼**로 복사된다.
3. **Segmentation (세그먼트화)**:
   - TCP는 버퍼의 데이터를 **MSS(Maximum Segment Size)** 단위로 쪼개어 '번호표(Sequence Number)'를 붙인 **세그먼트**를 만든다. (비유: 직소 퍼즐 조각)
4. **Encapsulation (캡슐화)**:
   - **L3(IP 패킷)**: 세그먼트를 택배 박스(패킷)에 담아 목적지 주소를 적는다.
   - **L2(Frame)**: 패킷을 트럭(프레임)에 실어 인접한 장치로 보낸다.

---

## 2. 네트워크 구간의 특징: "패킷 vs 프레임"

* **패킷(Packet)**: 최종 목적지까지 변하지 않는 **택배 박스**.
* **프레임(Frame)**: 구간마다 갈아타는 **트럭**. 라우터를 거칠 때마다 기존 프레임은 소멸하고 새로운 프레임(트럭)에 패킷이 실린다.

---

## 3. 수신 측(Receiver)의 제어 핵심: "Window Size"

TCP의 신뢰성 보장을 위한 핵심 메커니즘.

1. **ACK (Acknowledgement)**:
   - 수신 측은 데이터를 받을 때마다 "n번까지 잘 받았으니 n+1번을 달라"는 신호를 보낸다.
2. **Window Size (수신 창 크기)**:
   - ACK를 보낼 때, 현재 자신의 **커널 수신 버퍼에 남아있는 빈 공간** 크기를 서버에 알려준다.
3. **흐름 제어**:
   - 송신 측(서버)은 수신 측이 알려준 **Window Size**보다 큰 데이터를 절대 보내지 않는다. 수신 측 바구니가 꽉 차면 서버는 송신을 중단하고 대기.


### 🚦 TCP 혼잡 제어

1. **개념**: 데이터 송신량이 네트워크가 처리할 수 있는 용량을 초과하여 패킷 유실이 발생하는 것을 방지하는 메커니즘.
2. **흐름 제어 vs 혼잡 제어**:
   - **흐름 제어**: 송신자-수신자 간의 속도 조절 (Window Size 활용).
   - **혼잡 제어**: 송신자-네트워크 간의 속도 조절 (Congestion Window 활용).
3. **주요 알고리즘**:
   - **Slow Start**: 네트워크 통로의 용량을 확인하기 위해 전송량을 기하급수적으로 늘림.
   - **Multiplicative Decrease**: 패킷 유실 감지 시, 네트워크 붕괴를 막기 위해 송신 속도를 즉각적으로 크게 줄임.
4. **서버 개발자의 통찰**:
   - 서버 패킷 전송이 갑자기 느려진다면, 상대방 메모리(흐름 제어) 문제뿐만 아니라 **중간 경로의 라우터/스위치 과부하(혼잡 제어)** 가능성도 고려해야 함.


---

## 4. 성능 병목의 실체: "Read Speed"

네트워크 속도가 느려지는 가장 흔하지만 간과하기 쉬운 원인.

* **현상**: 네트워크 선로와 서버 사양은 충분한데 전송 속도가 급감하는 경우.
* **원인**: 수신측 애플리케이션의 **Read 속도** 저하.
  - 프로그램이 CPU 부하 등으로 바빠서 커널 수신 버퍼에 쌓인 데이터를 제때 `recv()`로 퍼 올리지 못함.
  - 수신 버퍼가 꽉 차면서 **Window Size가 0**이 됨.
  - 서버는 윈도우가 열릴 때까지 데이터를 보내지 못하고 대기함 (**네트워크 지연 발생**).

---

## 5. 개발자를 위한 최적화

1. **소켓 프로그래밍 전략**:
   - 데이터를 **수신하는 스레드**와 **처리(연산)하는 스레드**을 분리해야 한다.
   - 수신 스레드는 오직 커널 버퍼에서 유저 버퍼로 데이터를 최대한 빨리 퍼 올리는 일에만 집중해야 전송 속도를 유지할 수 있다.
   
2. **트러블슈팅**:
   - "네트워크가 느리다"는 불만이 나오면 **와이어샤크(Wireshark)** 등을 통해 **Window Size**의 변화를 확인해보자. 윈도우 크기가 지속적으로 줄어든다면 범인은 네트워크가 아니라 '받는 쪽의 로직'.

3. **메모리와의 연결**:
   - 64bit 시스템에서 대용량 RAM을 활용해 수신 버퍼 크기를 넉넉히 잡고, CPU 캐시 지역성을 고려한 빠른 로직 처리가 결국 네트워크 성능으로 직결됩니다.

### reference : [이해하면 인생이 바뀌는 TCP 송/수신 원리](https://www.youtube.com/watch?v=K9L9YZhEjC0&list=WL&index=1&t=1622s)

